FROM node:boron

#RUN apt-get update
#RUN apt-get install -y ruby-full
#RUN gem install sass
#RUN npm install -g vorlon

# Create app directory
RUN mkdir -p /usr/src/app
WORKDIR /usr/src/app

# Install app dependencies
COPY package.json /usr/src/app/
RUN npm install

# Bundle app source
COPY . /usr/src/app

EXPOSE 4200
#EXPOSE 1337
#EXPOSE 5050
CMD ["npm", "run", "start_qual"]
